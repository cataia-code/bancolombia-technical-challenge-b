# Dependencias de ejecucion (equivalen a [project.dependencies] de pyproject.toml).
# La capa LLM (anthropic) es OPCIONAL: la solucion corre sin ella.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
rapidfuzz>=3.6
jinja2>=3.1
typer>=0.12
